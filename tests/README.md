# Testes

Execute na raiz do projeto:

```powershell
py -3.13 -m unittest discover -s tests -v
```

Os testes públicos não usam uma ROM comercial. Testes de integração com ROM devem ser feitos localmente com uma cópia compatível fornecida pelo próprio usuário.
